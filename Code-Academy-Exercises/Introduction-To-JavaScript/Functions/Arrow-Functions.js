const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};

/* went over arrow functions to remove the need to type out the keyword function */
